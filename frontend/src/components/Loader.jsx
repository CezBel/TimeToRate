import { HashLoader } from 'react-spinners';

const Loader = () => {
  return (
    <HashLoader  
      className='loader'
      size={150}
      color='#ffc900' 
    />
  );
};
export default Loader;